% =============================================================================
% ABSTRACT - MAJOR REVISION NEEDED
% =============================================================================
% STATUS: Needs complete rewrite to reflect new framing
%
% OLD FRAMING (problematic):
%   - Heavy IR theory (Waltz, Wendt, 30 years of debate)
%   - "Neorealist vs Constructivist agents" → circular (we defined them differently)
%   - Confounds architecture and goal structure
%
% NEW FRAMING (security dilemma focused):
%   - Lead with the PROBLEM: security dilemmas in multi-agent systems
%   - Frame as: can semantic reasoning escape dilemmas that trap optimization?
%   - Two-comparison design isolates variables
%   - Broader relevance (beyond IR)
%
% STRUCTURE FOR NEW ABSTRACT (~100-150 words):
%   1. PROBLEM: Security dilemmas trap agents in suboptimal arms races
%   2. GAP: Unknown whether reasoning architecture affects ability to escape
%   3. APPROACH: Compare RL (optimization) vs LLM (semantic reasoning) in
%      minimal coordination game with identical incentive structures
%   4. DESIGN: Three configurations - RL, LLM-Control (same goal), LLM-Constructivist
%      Two comparisons isolate architecture vs framing effects
%   5. CONTRIBUTION: Informs multi-agent coordination + theoretical debates
%
% TYPOS TO FIX:
%   - "comparitive" → "comparative"
%   - "Reinforcemnt" → "Reinforcement"
%
% EXAMPLE DIRECTION (don't copy, write in your voice):
%   "In multi-agent systems without central authority, security dilemmas can
%   trap agents in suboptimal arms races. This thesis tests whether semantic-
%   reasoning agents (LLMs) can escape these dilemmas more readily than
%   optimization-based agents (RL). Using a minimal coordination game with
%   investment, arming, and conflict, we compare three agent configurations
%   to isolate the effects of decision-making architecture from goal framing.
%   Results inform both AI multi-agent coordination and theoretical debates
%   about whether reasoning processes can overcome material incentive structures."
%
% KEY CHANGE: Don't lead with IR theory. Lead with the problem.
% IR context (Waltz/Wendt) can be mentioned briefly but isn't the focus.
% =============================================================================

\begin{abstract}
Thirty years since A. Wendt's Constructivism challenged K. Waltz's Neorealism both International Relations (IR) theories are acknowledged as major pillars to the field. Although Agent-based modelling (ABM) has long made its way into IR, direct comparisons in the same simulation between neorealism and constructivism have been absent. Recent advancements in Artificial Intelligence allow for a comparitive study as constructivist actor macro-behaviour can be modelled through Large-Language Model (LLM) agents. Reinforcemnt Learning (RL) agents represent materialistic Neorealist actors.

My thesis will explore under what material conditions the representations of Neorealistic actors and Constructivist actors will produce divergent emergent social structures.

\end{abstract}
